# Typus Models Configuration File
#
# Use the README file as a reference to customize settings.

Proposal:
  fields:
    list: name, tractis_template_code, position
    form: name, problem, howto_solve, tractis_template_code
  order_by: position
  relationships: 
  filters: 
  search: 
  application: mifirma

Signature:
  fields:
    list: proposal, email, state, created_at
    form: proposal_id
  order_by: 
  relationships: 
  filters: 
  search:
  options:
    default_action: show
  application: mifirma

