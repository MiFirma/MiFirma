<%= content_for :bodyclass do %>proposal<% end %> 

<%= link_to "Compartir firma", share_signature_url(@signature.token), :rel => "facebox", :id => "social_share", :style => "display:none;" %>
<% javascript_tag do %>
  $(document).ready(function () {
    $("#social_share").trigger('click');
  });
<% end %>

<div class="page">

  <% if flash[:error] %>
		<div class="sign_error">
			<strong>Error al crear la firma.</strong>
			<%= flash[:error].html_safe %>
		</div>
  
	<% else %>
	
		<% if @signature.signed? %>
			<h1>Gracias por firmar! Ya estamos cambiando las cosas!</h1>
		<% else %>
			<h1>
				Firma incorrecta, vuelva a intentarlo pulsando aquí: <%= link_to "Firmar", @signature.tractis_contract_location %>    
			</h1>
		<% end %>
	<% end %>

</div>

<div class="clear">&nbsp;</div>
</div>